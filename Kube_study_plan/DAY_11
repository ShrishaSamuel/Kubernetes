Study
Scales based on CPU/memory/custom metrics via metrics-server / Prometheus Adapter.
Algorithm: desiredReplicas = ceil(currentReplicas * (currentMetric / targetMetric)).
HPA needs requests set on the pod.

Hands-On
kubectl apply -f https://github.com/kubernetes-sigs/metrics-server/releases/latest/download/components.yaml
kubectl autoscale deployment web --cpu-percent=50 --min=2 --max=10
kubectl run loadgen --image=busybox -it --rm -- /bin/sh -c \
  "while true; do wget -q -O- http://web; done"
kubectl get hpa -w

Interview Qs
How does HPA calculate desired replicas?
Why must requests be set for HPA to work on CPU?
HPA vs VPA vs Cluster Autoscaler.
How do you scale on custom metrics (e.g., queue length)?
What is the cooldown / stabilization window?

